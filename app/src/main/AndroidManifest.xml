<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureWeather"
        tools:targetApi="31">
        <activity
            android:name=".captcha.CaptchaChatbotActivity"
            android:exported="false" />
        <activity
            android:name=".captcha.CaptchaFindColorActivity"
            android:exported="false" />
        <activity
            android:name=".captcha.CaptchaCompassActivity"
            android:exported="false" />
        <activity
            android:name=".weather.WeatherActivity"
            android:exported="false" />
        <activity
            android:name=".captcha.BaseCaptchaActivity"
            android:exported="false" />
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".auth.RegistrationActivity"
            android:exported="false"
            android:theme="@style/Theme.SecureWeather" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SecureWeather">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>